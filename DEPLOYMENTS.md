# Deployments

| Date       | Target     | Env | Branch | Commit  | Notes                                              |
|------------|------------|-----|--------|---------|----------------------------------------------------|
| 2026-04-13 | homeserver | tst | tst    | 24304c1 | P-0003 dynamic MCP port allocation, merged dev->tst |
| 2026-04-13 | homeserver | prd | main   | 24304c1 | First PRD deployment — full platform (P-0001 + P-0002 + P-0003) |
| 2026-04-24 | homeserver | tst | tst    | 63b3ea9 | P-0004 env-scoped per-app systemd units + bootstrap docs. Service restart OK, all smoke endpoints green (/ /health /api/apps /api/system/metrics /dashboard). Pre-P-0004 user-scope units confirmed absent before restart. |
| 2026-04-25 | homeserver | tst | tst    | a4363ed | P-0005 Activate Systemd Per-App Services (all 3 scopes). Live deploy uncovered four spec gaps that required hotfixes (committed on dev and merged forward): (1) main-platform unit needs `Environment=XDG_RUNTIME_DIR=/run/user/1000` so system-scope can reach felipe's user-scope systemd; manually applied to `/etc/systemd/system/latarnia-tst.service`. Deployment skill bootstrap now documents the line. (2) `ServiceManager.start_service` was a thin systemctl wrapper — rewrote it as a one-shot (port alloc + create unit file + daemon-reload + start) to match `SubprocessLauncher.start_service`. (3) `RedisConfig.password` is optional on this Pi; switched to `getattr`. (4) `User=` removed from generated user-scope units (was causing status=216/GROUP); added `--redis-url` to ExecStart for verb parity with subprocess. Live cap verification on TST: cap-003 ✅ `/api/apps/{id}/process/start` creates `latarnia-tst-example-full-app.service` (active running). cap-005 ✅ `overall_status=green` once HealthMonitor is started. cap-006 ✅ `kill -9` triggers respawn within ~5s, PID changes, status returns to active. cap-007 ✅ linger=yes confirmed. Note: HealthMonitor is opt-in via `POST /api/health/start` — not auto-started in lifespan. PRD unit file NOT yet patched with XDG_RUNTIME_DIR; recommend doing this before any PRD deploy. |
| 2026-04-26 | homeserver | tst | tst    | 6b8627d | Auto-start HealthMonitor in lifespan (follow-up). After this, /api/apps overall_status flips to green ~30s after a fresh start without needing POST /api/health/start. cap-003/005/006 re-verified: kill -9 → respawn (PID 607485 → 607537), all smoke endpoints 200. Known gap (separate from this fix): on platform restart the in-memory app registry doesn't reconcile with surviving per-app systemd units; manually-started apps need to be restarted via the API after a platform restart. Worth a future scope. |
| 2026-04-26 | homeserver | prd | main   | fe81a2a | First PRD deploy of P-0005 (merged tst → main). Patched `/etc/systemd/system/latarnia-prd.service` with `Environment=XDG_RUNTIME_DIR=/run/user/1000` (mirror of the TST fix), `daemon-reload`, restarted. PRD smoke green: /health (good), /dashboard 200, /api/system/metrics 200, /api/apps 200 (no apps — PRD only deploys real apps, none installed yet), /api/health 200, linger=yes, HealthMonitor auto-started. Pre-existing route bug noticed: `/api/health/status` and `/api/health/statistics` 500 because they fall into the `/api/health/{app_id}` route and the bare `except Exception` references an undefined `logger` — unrelated to P-0005. |
| 2026-04-30 | homeserver | tst | tst    | a69d969 | P-0006 Secret Manager v1. Bootstrap: created /opt/latarnia/tst/secrets.env (mode 600) with EXAMPLE_API_KEY. Smoke: /health good, GET /api/secrets returns EXAMPLE_API_KEY with used_by=["example-full-app"], no values in response. example_full_app now declares requires_secrets: ["EXAMPLE_API_KEY"]. |
| 2026-04-30 | homeserver | prd | main   | a69d969 | P-0006 Secret Manager v1. Bootstrap: created /opt/latarnia/prd/secrets.env (mode 600, empty — no apps in PRD). Smoke: /health good, GET /api/secrets returns empty list (no secrets configured, no apps). |
| 2026-04-26 | homeserver | tst | tst    | 95041f5 | P-0005 Scope 4 follow-ups: (1) drop PartOf= from generated unit (cross-scope no-op); apps now intentionally survive platform restarts. (2) Startup reconciliation: ServiceManager.reconcile_running_units() parses --port/--mcp-port from active per-app units' ExecStart, claims them in PortManager, marks apps RUNNING. (3) Drop --logs-dir; journald is the canonical log sink on Linux. /api/apps/{id}/logs dispatches by OS+type. Manifest `logs_dir` field deprecated. example_full_app cleaned up. (4) app-specification.md updated. Plus journalctl hotfix: query system journal via `_SYSTEMD_USER_UNIT={unit}` instead of `--user -u` (Pi has no user-mode persistent journald). Live verification on TST after platform restart: reconciliation picked up surviving example-full-app (port=8100, mcp_port=9001) → overall_status flipped to green automatically. cap-006 respawn verified (PID 618759 → 618972). /api/apps/{id}/logs now returns journald entries with source="journald". All smoke endpoints 200. |
| 2026-04-26 | homeserver | prd | main   | 32a5189 | PRD promotion of P-0005 Scope 4 follow-ups (PartOf drop + reconciliation + journald-only logs + journalctl system-journal fix). Pulled main, restarted latarnia-prd.service, smoke green: /health (good), /dashboard 200, /api/system/metrics 200, /api/apps 200 (0 apps), /api/health 200. Reconciliation no-op (no per-app units to reconcile yet). The PRD unit file's XDG_RUNTIME_DIR=/run/user/1000 from the previous deploy is still in place and works as expected. |
| 2026-04-26 | homeserver | tst | tst    | e79215f | P-0005 housekeeping: (a) reordered /api/health routes so static paths (/status, /statistics, /config) precede the dynamic /{app_id} — fixes the long-standing 500 on GET /api/health/status. (b) Added module-level `logger` in main.py so endpoint error paths actually log instead of NameError. (c) System docs (architecture.md, workflows.md, SYSTEM.md) refreshed to match Scope 4: no PartOf, reconciliation flow, journald log dispatch table, P-0005 row marked plain [DONE]. Verified live on TST: /api/health/status returns 200, /api/health/statistics returns 200 with real numbers (total_apps=1, healthy_apps=1), /api/health/nonexistent returns proper 404. |
| 2026-04-26 | homeserver | prd | main   | bdcf9dd | PRD promotion of housekeeping fixes (route order + module-level logger + System doc refresh). Smoke verified live: /api/health/status returns 200 (was 500 since first PRD deploy), /api/health/statistics returns 200 (total_apps=0 — PRD has no apps yet). |
| 2026-04-29 | homeserver | tst | tst    | a639fee | pgvector as platform-default Postgres extension. Installed `postgresql-17-pgvector` on the Pi (`sudo apt install`), one-time host setup. db_provisioner.py now runs `CREATE EXTENSION IF NOT EXISTS vector` for every per-app DB on every provisioning pass (idempotent; existing DBs get backfilled). Failures log a WARNING and don't fail provisioning. App-spec documents the extension and the future-list pattern (`DEFAULT_EXTENSIONS` in `db_provisioner.py`). Live verification on TST: post-restart log shows `Ensured extension 'vector' on tst_latarnia_example_full_app`; `SELECT '[1,2,3]'::vector(3) <-> '[1,1,1]'::vector(3)` returns 2.2360... — pgvector operators work. |
| 2026-04-29 | homeserver | prd | main   | 897ac95 | PRD promotion of pgvector base extension. `postgresql-17-pgvector` installed on the host (shared cluster — same install as TST). PRD has no app DBs yet, so reconciliation no-op; the binary and code are in place for any future PRD app that needs vectors. Pulled main, restarted latarnia-prd.service, smoke active. |
| 2026-05-11 | homeserver | tst | tst    | 6d25972 | Bug fixes merged to TST: (1) data-dir relative path fix in app_manager, (2) health monitor stuck-in-error and startup-race fixes, (3) stale subprocess_launcher test fixed (sys.executable assertion). 332 regression tests pass. Smoke: /health good, /api/secrets returns EXAMPLE_API_KEY, service active. |
| 2026-05-11 | homeserver | tst | tst    | b8966f5 | Fix /api/system/restart stub — now fires `sudo systemctl restart latarnia-tst.service` on Linux (1s async delay so HTTP response escapes first); no-op on Darwin. Verified: POST /api/system/restart returns `{"success":true,"message":"Restart initiated for latarnia-tst.service"}`, service cycled and came back with `health: good`. 332 regression tests pass. |
