{
  "name": "example_full_app",
  "type": "service",
  "description": "Full-featured example app demonstrating all Latarnia capabilities, including P-0008 role-aware behaviour: the webUI and MCP tools adapt to the X-Latarnia-App-Role header (no new manifest fields required).",
  "version": "1.0.0",
  "author": "Latarnia",
  "main_file": "app.py",
  "config": {
    "has_UI": true,
    "has_web_ui": true,
    "redis_required": true,
    "database": true,
    "mcp_server": true,
    "data_dir": true,
    "auto_start": false,
    "restart_policy": "always",
    "redis_streams_publish": ["example.events.created"],
    "redis_streams_subscribe": ["example.commands.process"],
    "requires_secrets": ["EXAMPLE_API_KEY"],
    "public_routes": ["/b/"]
  },
  "requires": [
    {"app": "example_companion", "min_version": "1.0.0"}
  ]
}
